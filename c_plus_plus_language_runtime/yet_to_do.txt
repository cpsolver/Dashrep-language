Yet to do for converting runtime from Perl to C++


-------------------------------------------------

keys( %

10 matches across 6 files

for (const auto &key_value_pair : global_number_of_times_encountered_phrase_named) {
    const std::string &phrase_name = key_value_pair.first;
    // ... body using phrase_name (and optionally key_value_pair.second for the count)


-------------------------------------------------

split(

62 matches across 44 files

#include <iterator>

std::string global_temp_text = /* ... */;
std::regex re{" +"}; // one or more spaces
std::sregex_token_iterator it(global_temp_text.begin(), global_temp_text.end(), re, -1);
std::sregex_token_iterator end;
std::vector<std::string> globallist(it, end);

Perl:
$globallist = split( / +/ , $global_temp_text );

faster, just splits on spaces:

std::string s = global_temp_text;
std::vector<std::string> globallist;
size_t i = 0, n = s.size();
while (i < n) {
    while (i < n && s[i] == ' ') ++i;             // skip spaces
    if (i >= n) break;
    size_t j = i;
    while (j < n && s[j] != ' ') ++j;             // find next space
    globallist.emplace_back(s.substr(i, j - i));
    i = j;
}


-------------------------------------------------

declare globally:
trace_file
as
std::ofstream trace_file


-------------------------------------------------

fix code snippet that removes combee comenn coments


add code snippet that does diff list one versus list two


-------------------------------------------------


create:
dashrep_adjust_compiler_generate_cpp_code.txt

move code that inserts libraries and function declarations

do replacements as needed, especially for new phrases


-------------------------------------------------

insert when needed:
template-function-cgi-decode
needed by functions that call cgi_decode


-------------------------------------------------

convert functions needed by simple Dashrep programs.
Later, fix other functions.


-------------------------------------------------

string-two-apostrophes

fix to use quotation marks


-------------------------------------------------

generate-list-of-tokens-in-current-definition-quotable


-------------------------------------------------

define-special-strings-that-are-padded-with-spaces


-------------------------------------------------
